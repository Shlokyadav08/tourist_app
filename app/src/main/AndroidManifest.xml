<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.CitySightseeing"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.App.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
            <activity android:name=".LoginActivity" android:exported="true" android:theme="@style/Base.Theme.CitySightseeing"/> <!-- Ensure a theme is applied -->
            <activity android:name=".RegisterActivity" android:exported="true" android:theme="@style/Base.Theme.CitySightseeing"/> <!-- Ensure a theme is applied -->
            <activity android:name=".DashboardActivity" android:exported="true" android:theme="@style/Base.Theme.CitySightseeing"/> <!-- Ensure a theme is applied -->
            <activity android:name=".LoadingActivity" android:exported="true" android:theme="@style/Base.Theme.CitySightseeing"/> <!-- Ensure a theme is applied -->
            <activity android:name=".ForgotPasswordActivity" android:exported="true" android:theme="@style/Base.Theme.CitySightseeing"/> <!-- Ensure a theme is applied -->
            <activity android:name=".SearchActivity" android:exported="true" android:theme="@style/Base.Theme.CitySightseeing"/> <!-- Ensure a theme is applied -->
            <activity android:name=".HomeActivity" android:exported="true" android:theme="@style/Base.Theme.CitySightseeing"/> <!-- Ensure a theme is applied -->
            <activity android:name=".MessagesActivity" android:exported="true" android:theme="@style/Base.Theme.CitySightseeing"/> <!-- Ensure a theme is applied -->
            <activity android:name=".ProfileActivity" android:exported="true" android:theme="@style/Base.Theme.CitySightseeing"/> <!-- Ensure a theme is applied -->
            <activity android:name=".ResetPasswordActivity" android:exported="true" android:theme="@style/Base.Theme.CitySightseeing"/> <!-- Ensure a theme is applied -->
            <activity android:name=".CalendarActivity" android:exported="true" android:theme="@style/Base.Theme.CitySightseeing"/> <!-- Ensure a theme is applied -->
            <activity android:name=".OtpActivity" android:exported="true" android:theme="@style/Base.Theme.CitySightseeing"/> <!-- Ensure a theme is applied -->
            <activity android:name=".SmsBroadcastReceiverActivity" android:exported="true" android:theme="@style/Base.Theme.CitySightseeing"/> <!-- Ensure a theme is applied -->
    </application>
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />

</manifest>